let groceryList = ['Arroz', 'Feijão', 'Alface', 'Melancia'];

groceryList.forEach((value, index) => console.log(index + 1 + ": " + value));

let names = ['João', 'Maria', 'Antônio', 'Margarida'];

for (name of names) {
  console.log(name);
}