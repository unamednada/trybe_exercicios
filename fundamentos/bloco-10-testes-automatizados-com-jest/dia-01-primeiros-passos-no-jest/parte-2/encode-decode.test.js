const { encode, decode } = require('./encode-decode.js');

describe('Verifica se encode se comporta como esperado', () => {



})

describe('Verifica se encode se comporta como esperado', () => {



})